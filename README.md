# fix-cli

Rust FIX Client CLI project.

当前阶段只初始化项目目录，等待 ChatGPT 完成纯 Rust 路线 B 的设计方案后再开始开发。

## 当前状态

- 项目目录已建立
- 尚未开始实现 FIX codec、SessionActor 或 CLI 功能
- 尚未确定最终 Cargo 依赖和模块结构

## 规划

后续将根据确认后的纯 Rust 方案实现 `fixctl` CLI 与 `fixd` FIX 会话服务，并在开发前补充测试和验证标准。
